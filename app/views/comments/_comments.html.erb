<hr><div class="container">
	<h3>Comments</h3>

	<% commentable.comments.each do |comment| %>
		<div class="well">
			<%= comment.user.name %> <%= comment.user.surname %>  |
			<%= comment.body %> | 
			<%= comment.created_at %>
		</div>
	<% end %>
</div>